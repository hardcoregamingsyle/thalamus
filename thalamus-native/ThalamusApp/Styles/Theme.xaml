<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!-- ═════════════════════════════════════════════════════════════════════
         THALAMUS THEME — single source of truth for the desktop apps.
         Mirrors the website palette in src/index.css (near-black blue
         background, dark cards, electric-blue primary, muted secondary text).
         Shared by ThalamusApp and ThalamusInstaller (linked as a Page).
         ═════════════════════════════════════════════════════════════════════ -->

    <!-- ── Colors — neutral soft-black (Claude Code dark), chroma 0. Matches
         src/index.css after the blue tint was removed. Only accents carry hue. -->
    <Color x:Key="BgDeep">#181818</Color>        <!-- css sidebar        oklch(0.11 0 0) -->
    <Color x:Key="BgDarker">#141414</Color>      <!-- css background     oklch(0.10 0 0) -->
    <Color x:Key="BgCard">#1e1e1e</Color>        <!-- css card           oklch(0.13 0 0) -->
    <Color x:Key="BgInput">#1c1c1c</Color>       <!-- css input          oklch(0.15 0 0) -->
    <Color x:Key="BgHover">#242424</Color>       <!-- hover surface -->
    <Color x:Key="BgActive">#2c2c2c</Color>      <!-- active nav, one step above hover -->
    <Color x:Key="BorderColor">#3a3a3a</Color>   <!-- css border          oklch(0.26 0 0) -->
    <Color x:Key="BorderDim">#2a2a2a</Color>     <!-- css sidebar-border  oklch(0.22 0 0) -->
    <!-- rgba(255,255,255,0.10) — the website's dominant border-white/10 hairline -->
    <Color x:Key="BorderSubtle">#1AFFFFFF</Color>
    <Color x:Key="TextPrimary">#ececec</Color>   <!-- css foreground      oklch(0.95 0 0) -->
    <Color x:Key="TextSecondary">#c4c4c4</Color> <!-- css secondary-fg    oklch(0.82 0 0) -->
    <Color x:Key="TextMuted">#8a8a8a</Color>     <!-- css muted-fg        oklch(0.58 0 0) -->
    <Color x:Key="BlueAccent">#0088f1</Color>    <!-- css primary         oklch(0.62 .20 250) -->
    <Color x:Key="BlueGlow">#0073d2</Color>      <!-- deeper primary for glows/gradients -->
    <Color x:Key="GreenAccent">#10b981</Color>
    <Color x:Key="AmberAccent">#e78c08</Color>   <!-- css accent          oklch(0.72 .16 65) -->
    <Color x:Key="PurpleAccent">#8b5cf6</Color>
    <Color x:Key="RedAccent">#de1c4e</Color>     <!-- css destructive     oklch(0.58 .22 15) -->

    <!-- ── Brushes ── -->
    <SolidColorBrush x:Key="BgDeepBrush"        Color="{StaticResource BgDeep}"/>
    <SolidColorBrush x:Key="BgDarkerBrush"      Color="{StaticResource BgDarker}"/>
    <SolidColorBrush x:Key="BgCardBrush"        Color="{StaticResource BgCard}"/>
    <SolidColorBrush x:Key="BgInputBrush"       Color="{StaticResource BgInput}"/>
    <SolidColorBrush x:Key="BgHoverBrush"       Color="{StaticResource BgHover}"/>
    <SolidColorBrush x:Key="BgActiveBrush"      Color="{StaticResource BgActive}"/>
    <SolidColorBrush x:Key="BorderBrush"        Color="{StaticResource BorderColor}"/>
    <SolidColorBrush x:Key="BorderDimBrush"     Color="{StaticResource BorderDim}"/>
    <SolidColorBrush x:Key="BorderSubtleBrush"  Color="{StaticResource BorderSubtle}"/>
    <SolidColorBrush x:Key="TextPrimaryBrush"   Color="{StaticResource TextPrimary}"/>
    <SolidColorBrush x:Key="TextSecondaryBrush" Color="{StaticResource TextSecondary}"/>
    <SolidColorBrush x:Key="TextMutedBrush"     Color="{StaticResource TextMuted}"/>
    <SolidColorBrush x:Key="BlueBrush"          Color="{StaticResource BlueAccent}"/>
    <SolidColorBrush x:Key="GreenBrush"         Color="{StaticResource GreenAccent}"/>
    <SolidColorBrush x:Key="AmberBrush"         Color="{StaticResource AmberAccent}"/>
    <SolidColorBrush x:Key="PurpleBrush"        Color="{StaticResource PurpleAccent}"/>
    <SolidColorBrush x:Key="RedBrush"           Color="{StaticResource RedAccent}"/>

    <!-- ── Typography — native stand-in for the site's Inter ── -->
    <FontFamily x:Key="AppFontFamily">Segoe UI Variable Text, Segoe UI</FontFamily>
    <!-- Monospace for the status strip, code blocks, and technical labels —
         reads like a dev tool, not a chat app. Cascadia Code ships with Windows
         Terminal / recent Windows; Consolas is the always-present fallback. -->
    <FontFamily x:Key="MonoFontFamily">Cascadia Code, Consolas</FontFamily>

    <!-- ── Gradients ── -->
    <LinearGradientBrush x:Key="BlueGradient" StartPoint="0,0" EndPoint="1,1">
        <GradientStop Color="#0088f1" Offset="0"/>
        <GradientStop Color="#0073d2" Offset="1"/>
    </LinearGradientBrush>

    <!-- Neutral near-black gradient stops (no blue channel bump). Surfaces stay
         at/below card (#1e1e1e) so cards read as elevated. -->
    <LinearGradientBrush x:Key="SidebarGradient" StartPoint="0,0" EndPoint="0,1">
        <GradientStop Color="#181818" Offset="0"/>
        <GradientStop Color="#1a1a1a" Offset="0.5"/>
        <GradientStop Color="#181818" Offset="1"/>
    </LinearGradientBrush>

    <LinearGradientBrush x:Key="ContentBgGradient" StartPoint="0,0" EndPoint="0,1">
        <GradientStop Color="#161616" Offset="0"/>
        <GradientStop Color="#141414" Offset="1"/>
    </LinearGradientBrush>

    <LinearGradientBrush x:Key="StatusBarGradient" StartPoint="0,0" EndPoint="0,1">
        <GradientStop Color="#161616" Offset="0"/>
        <GradientStop Color="#141414" Offset="1"/>
    </LinearGradientBrush>

    <LinearGradientBrush x:Key="CardGlow" StartPoint="0,0" EndPoint="0,1">
        <GradientStop Color="#212121" Offset="0"/>
        <GradientStop Color="#1e1e1e" Offset="1"/>
    </LinearGradientBrush>

    <!-- ── Card — dark surface, ~16px corners, hairline border ── -->
    <Style x:Key="Card" TargetType="Border">
        <Setter Property="Background" Value="{StaticResource BgCardBrush}"/>
        <Setter Property="BorderBrush" Value="{StaticResource BorderSubtleBrush}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="CornerRadius" Value="16"/>
    </Style>

    <!-- ── Example-prompt chip — rounded bordered button that prefills the input.
         Mirrors the website's empty-state suggestion chips (bg-card border). ── -->
    <Style x:Key="ExampleChip" TargetType="Border">
        <Setter Property="Background" Value="{StaticResource BgCardBrush}"/>
        <Setter Property="BorderBrush" Value="{StaticResource BorderSubtleBrush}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="CornerRadius" Value="9"/>
        <Setter Property="Padding" Value="12,8"/>
        <Setter Property="Margin" Value="0,0,8,8"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Background" Value="{StaticResource BgHoverBrush}"/>
                <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}"/>
            </Trigger>
        </Style.Triggers>
    </Style>

    <!-- Chip label — quiet secondary text sitting inside an ExampleChip. -->
    <Style x:Key="ExampleChipText" TargetType="TextBlock">
        <Setter Property="FontSize" Value="11.5"/>
        <Setter Property="Foreground" Value="{StaticResource TextSecondaryBrush}"/>
    </Style>

    <!-- ── Section header — muted uppercase label ── -->
    <Style x:Key="SectionHeader" TargetType="TextBlock">
        <Setter Property="FontSize" Value="10"/>
        <Setter Property="FontWeight" Value="Bold"/>
        <Setter Property="Foreground" Value="{StaticResource TextMutedBrush}"/>
    </Style>

    <!-- ── Primary button — blue gradient pill ── -->
    <Style x:Key="PrimaryBtn" TargetType="Button">
        <Setter Property="Background" Value="{StaticResource BlueGradient}"/>
        <Setter Property="Foreground" Value="White"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Height" Value="40"/>
        <Setter Property="Padding" Value="20,0"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="FontSize" Value="12.5"/>
        <Setter Property="FontWeight" Value="SemiBold"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="Bd" Background="{TemplateBinding Background}"
                            CornerRadius="10" Padding="{TemplateBinding Padding}">
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Bd" Property="Opacity" Value="0.88"/>
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="Bd" Property="Opacity" Value="0.75"/>
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter TargetName="Bd" Property="Opacity" Value="0.35"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- ── Danger button — destructive actions (uninstall, delete) ── -->
    <Style x:Key="DangerBtn" TargetType="Button" BasedOn="{StaticResource PrimaryBtn}">
        <Setter Property="Background" Value="{StaticResource RedBrush}"/>
    </Style>

    <!-- ── Ghost button — quiet outline ── -->
    <Style x:Key="GhostBtn" TargetType="Button">
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="Foreground" Value="{StaticResource TextSecondaryBrush}"/>
        <Setter Property="BorderBrush" Value="{StaticResource BorderDimBrush}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Height" Value="36"/>
        <Setter Property="Padding" Value="16,0"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="FontSize" Value="11.5"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="Bd" Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="8" Padding="{TemplateBinding Padding}">
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Bd" Property="Background" Value="{StaticResource BgHoverBrush}"/>
                            <Setter TargetName="Bd" Property="BorderBrush" Value="{StaticResource BorderBrush}"/>
                            <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}"/>
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter TargetName="Bd" Property="Opacity" Value="0.35"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- ── Text input — dark field, rounded, blue focus ring ── -->
    <Style x:Key="ThemedInput" TargetType="TextBox">
        <Setter Property="Background" Value="{StaticResource BgInputBrush}"/>
        <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}"/>
        <Setter Property="CaretBrush" Value="{StaticResource BlueBrush}"/>
        <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="FontSize" Value="13"/>
        <Setter Property="Padding" Value="14,10"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="TextBox">
                    <Border x:Name="Bd" Background="{TemplateBinding Background}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            CornerRadius="10">
                        <ScrollViewer x:Name="PART_ContentHost"
                                      Padding="{TemplateBinding Padding}"
                                      VerticalAlignment="Center"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsFocused" Value="True">
                            <Setter TargetName="Bd" Property="BorderBrush" Value="{StaticResource BlueBrush}"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- ── Checkbox — dark box, blue-gradient check ── -->
    <Style x:Key="ThemedCheck" TargetType="CheckBox">
        <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}"/>
        <Setter Property="FontSize" Value="12"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="Margin" Value="0,6,0,6"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="CheckBox">
                    <StackPanel Orientation="Horizontal" Background="Transparent">
                        <Border x:Name="Box" Width="16" Height="16" CornerRadius="4"
                                Background="{StaticResource BgInputBrush}"
                                BorderBrush="{StaticResource BorderBrush}"
                                BorderThickness="1" VerticalAlignment="Center">
                            <Path x:Name="Check" Data="M1 5 L4.5 8.5 L11 1.5"
                                  Stroke="White" StrokeThickness="1.8"
                                  StrokeStartLineCap="Round" StrokeEndLineCap="Round"
                                  StrokeLineJoin="Round"
                                  Width="10" Height="9" Stretch="Uniform"
                                  Visibility="Collapsed"
                                  HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ContentPresenter Margin="9,0,0,0" VerticalAlignment="Center"
                                          RecognizesAccessKey="True"/>
                    </StackPanel>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsChecked" Value="True">
                            <Setter TargetName="Box" Property="Background" Value="{StaticResource BlueGradient}"/>
                            <Setter TargetName="Box" Property="BorderBrush" Value="Transparent"/>
                            <Setter TargetName="Check" Property="Visibility" Value="Visible"/>
                        </Trigger>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Box" Property="BorderBrush" Value="{StaticResource BlueBrush}"/>
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Opacity" Value="0.45"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- ── Slider — thin track, round blue thumb ── -->
    <Style x:Key="ThemedSlider" TargetType="Slider">
        <Setter Property="Height" Value="18"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Slider">
                    <Grid VerticalAlignment="Center" Background="Transparent">
                        <Border Height="4" CornerRadius="2" Background="{StaticResource BgHoverBrush}"/>
                        <Track x:Name="PART_Track">
                            <Track.DecreaseRepeatButton>
                                <RepeatButton Command="Slider.DecreaseLarge" Focusable="False">
                                    <RepeatButton.Template>
                                        <ControlTemplate TargetType="RepeatButton">
                                            <Border Height="4" CornerRadius="2"
                                                    Background="{StaticResource BlueGradient}"/>
                                        </ControlTemplate>
                                    </RepeatButton.Template>
                                </RepeatButton>
                            </Track.DecreaseRepeatButton>
                            <Track.IncreaseRepeatButton>
                                <RepeatButton Command="Slider.IncreaseLarge" Focusable="False">
                                    <RepeatButton.Template>
                                        <ControlTemplate TargetType="RepeatButton">
                                            <Border Height="4" Background="Transparent"/>
                                        </ControlTemplate>
                                    </RepeatButton.Template>
                                </RepeatButton>
                            </Track.IncreaseRepeatButton>
                            <Track.Thumb>
                                <Thumb Width="14" Height="14" Cursor="Hand">
                                    <Thumb.Template>
                                        <ControlTemplate TargetType="Thumb">
                                            <Ellipse Fill="{StaticResource BlueBrush}"
                                                     Stroke="#0b1220" StrokeThickness="1"/>
                                        </ControlTemplate>
                                    </Thumb.Template>
                                </Thumb>
                            </Track.Thumb>
                        </Track>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- ── Thin scrollbar — 4px thumb, no arrows ── -->
    <Style x:Key="ThinScrollBar" TargetType="ScrollBar">
        <Setter Property="Width" Value="4"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ScrollBar">
                    <Track x:Name="PART_Track" IsDirectionReversed="true">
                        <Track.DecreaseRepeatButton>
                            <RepeatButton Command="ScrollBar.LineUpCommand" Opacity="0" Height="0"/>
                        </Track.DecreaseRepeatButton>
                        <Track.Thumb>
                            <Thumb>
                                <Thumb.Template>
                                    <ControlTemplate TargetType="Thumb">
                                        <Border Background="{StaticResource BorderBrush}"
                                                CornerRadius="2" Opacity="0.7"/>
                                    </ControlTemplate>
                                </Thumb.Template>
                            </Thumb>
                        </Track.Thumb>
                        <Track.IncreaseRepeatButton>
                            <RepeatButton Command="ScrollBar.LineDownCommand" Opacity="0" Height="0"/>
                        </Track.IncreaseRepeatButton>
                    </Track>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

</ResourceDictionary>
