<Window x:Class="ThalamusApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:wv2="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf"
        Title="Thalamus AI"
        Width="1400" Height="900"
        MinWidth="900" MinHeight="600"
        WindowStyle="None"
        AllowsTransparency="False"
        Background="#050a14"
        WindowStartupLocation="CenterScreen"
        StateChanged="Window_StateChanged">

    <Window.Resources>
        <Style x:Key="TitleBarButton" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="#64748b"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Width" Value="46"/>
            <Setter Property="Height" Value="32"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}" CornerRadius="4">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#1a2d4a"/>
                                <Setter Property="Foreground" Value="#e2e8f0"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="CloseButton" TargetType="Button" BasedOn="{StaticResource TitleBarButton}">
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#ef4444"/>
                    <Setter Property="Foreground" Value="White"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="36"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Custom Title Bar -->
        <Grid Grid.Row="0" Background="#020b1d" MouseLeftButtonDown="TitleBar_MouseLeftButtonDown">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <!-- Left: Logo + Title -->
            <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center" Margin="14,0,0,0">
                <Border Width="18" Height="18" CornerRadius="4" Background="#3b82f6" Margin="0,0,8,0">
                    <TextBlock Text="T" FontSize="10" FontWeight="Black" Foreground="White"
                               HorizontalAlignment="Center" VerticalAlignment="Center"/>
                </Border>
                <TextBlock Text="Thalamus AI" FontSize="12" FontWeight="SemiBold" Foreground="#e2e8f0"
                           VerticalAlignment="Center" Margin="0,0,8,0"/>
                <Border CornerRadius="10" Background="#0d1f3c" BorderBrush="#1e3a5f" BorderThickness="1" Padding="6,1">
                    <TextBlock Text="L4.5" FontSize="9" FontWeight="Bold" Foreground="#3b82f6"
                               VerticalAlignment="Center"/>
                </Border>
            </StackPanel>

            <!-- Right: Window Controls -->
            <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                <Button x:Name="MinimizeBtn" Style="{StaticResource TitleBarButton}"
                        Content="&#x2212;" Click="MinimizeBtn_Click" ToolTip="Minimize"/>
                <Button x:Name="MaximizeBtn" Style="{StaticResource TitleBarButton}"
                        Content="&#x25A1;" Click="MaximizeBtn_Click" ToolTip="Maximize"/>
                <Button x:Name="CloseBtn" Style="{StaticResource CloseButton}"
                        Content="&#x2715;" Click="CloseBtn_Click" ToolTip="Close"/>
            </StackPanel>
        </Grid>

        <!-- WebView2 -->
        <wv2:WebView2 x:Name="WebView" Grid.Row="1"
                      NavigationStarting="WebView_NavigationStarting"
                      NavigationCompleted="WebView_NavigationCompleted"
                      CoreWebView2InitializationCompleted="WebView_CoreWebView2InitializationCompleted"/>

        <!-- Loading overlay -->
        <Grid x:Name="LoadingOverlay" Grid.Row="1" Background="#050a14" Visibility="Visible">
            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                <Border Width="60" Height="60" CornerRadius="14" Background="#3b82f6" Margin="0,0,0,20">
                    <TextBlock Text="T" FontSize="28" FontWeight="Black" Foreground="White"
                               HorizontalAlignment="Center" VerticalAlignment="Center"/>
                </Border>
                <TextBlock Text="Thalamus AI" FontSize="22" FontWeight="Bold" Foreground="#e2e8f0"
                           HorizontalAlignment="Center" Margin="0,0,0,8"/>
                <TextBlock Text="World's First L4.5 Agent" FontSize="13" Foreground="#64748b"
                           HorizontalAlignment="Center" Margin="0,0,0,24"/>
                <ProgressBar x:Name="LoadingBar" Width="200" Height="3" IsIndeterminate="True"
                             Background="#1a2d4a" Foreground="#3b82f6"/>
                <TextBlock x:Name="LoadingStatus" Text="Starting..." FontSize="11" Foreground="#64748b"
                           HorizontalAlignment="Center" Margin="0,12,0,0"/>
            </StackPanel>
        </Grid>
    </Grid>
</Window>
