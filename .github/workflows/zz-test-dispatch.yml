name: zz-test-dispatch
on:
  workflow_dispatch:
    inputs:
      note:
        required: false
        type: string
jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo "ok ${{ github.event.inputs.note }}"
